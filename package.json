{
  "name": "sample-node-app",
  "version": "1.0.0",
  "description": "Sample project to kick start nodejs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  }
}
